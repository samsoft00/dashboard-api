export default class ActivityLogController {
  // getActivityLogs
  // getUserActivityLogs
  // getLoanActivityLogs
  // getFxActivityLogs
}

// add logs to users by id
// routes
// --------------
// api/v1/activity-logs <- show all activities logs
// api/v1/activity-logs/user/:user_id -> show user logs
// api/v1/activity-logs/loan/:loan_id -> show loan logs
// api/v1/activity-logs/fxorder/:fx_id -> show fx logs

/*
- Typescript
- React
- Node
- GraphQL

node.js
reactjs
typescript
javascript    
*/

// inflow - Remove the currency in front of the digit
// connect loan to mifos once complete
// Reject button for Operation
// Intgration between dashboard
// bvn integration ~ NIPS

// Hey, good afternoon, a quick request came in for the dashbaord, I think we can knock it out for them. They want the same Applicant kind of view for FX orders, so we need an API that returns unique customer info across the fx orders table and another one that shows all the orders they have made with us when a customer is clicked
